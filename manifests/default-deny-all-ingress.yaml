# Recipe 03: DENY all non-whitelisted traffic in the namespace
# Do not apply to system namespaces (e.g. kube-system, openshift-*).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: default
spec:
  podSelector: {}
  ingress: []
